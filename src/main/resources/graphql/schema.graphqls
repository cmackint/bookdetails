type Query {
  getBookById(bookId: ID): Book
}

type Book {
  bookId: ID
  name: String
  author: Author
  pageCount: Int
}

type Author {
  authorId: ID
  firstName: String
  lastName: String
}
